<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>インターン研究ノート ｜ MY CAREER LOG</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Yomogi&family=Zen+Maru+Gothic:wght@500;700&family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#FBF6EE;
    --surface:#FFFDF8;
    --ink:#4A3F35;
    --ink-soft:#8A7C6C;
    --border:#E9DCC6;
    --accent:#C98A5B;      /* 企業情報 */
    --accent-soft:#F7E7D4;
    --sec-b:#8C86C9;       /* インターン内容 */
    --sec-b-soft:#EEECF9;
    --sec-c:#E29A9A;       /* 感想・振り返り */
    --sec-c-soft:#FBEBEA;
    --sec-d:#8FB98A;       /* 価値観・成長 */
    --sec-d-soft:#EAF3E8;
    --sec-e:#7FA7C9;       /* つながり・ワードクラウド */
    --sec-e-soft:#E9F1F7;
    --focus:#C98A5B;
    --radius:18px;
  }
  *{ box-sizing:border-box; }
  html{ scroll-behavior:smooth; }
  body{
    margin:0;
    background:
      radial-gradient(circle at 6% 8%, rgba(226,154,154,0.16) 0, transparent 38%),
      radial-gradient(circle at 96% 14%, rgba(140,134,201,0.14) 0, transparent 40%),
      var(--bg);
    color:var(--ink);
    font-family:"Noto Sans JP", sans-serif;
    line-height:1.8;
    font-size:15px;
  }
  h1,h2,h3{ font-family:"Zen Maru Gothic","Noto Sans JP",sans-serif; margin:0; }
  .hand{ font-family:"Yomogi","Zen Maru Gothic",sans-serif; }
  a{ color:var(--accent); }
  button{ font-family:inherit; }

  /* ---- header ---- */
  header{ background:var(--surface); border-bottom:2px dashed var(--border); padding:1.1rem 1.2rem 0.6rem; }
  .header-inner{ max-width:1140px; margin:0 auto; display:flex; align-items:center; gap:0.9rem; }
  .fox-mascot{ flex:none; width:54px; height:54px; object-fit:cover; object-position:center; border-radius:50%; }
  header h1{ font-size:1.15rem; }
  header p.sub{ margin:0.2rem 0 0; font-size:0.8rem; color:var(--ink-soft); }

  /* ---- main nav (views) ---- */
  nav.stepnav{ position:sticky; top:0; z-index:30; background:var(--surface); border-bottom:2px dashed var(--border); }
  .stepnav-inner{
    max-width:1140px; margin:0 auto; display:flex; overflow-x:auto; scrollbar-width:none;
    padding:0 0.4rem;
  }
  .stepnav-inner::-webkit-scrollbar{ display:none; }
  .stepnav-inner button{
    flex:none; text-align:center; padding:0.7rem 0.85rem; font-size:0.8rem; font-weight:700;
    background:none; border:none; border-bottom:3px solid transparent; color:var(--ink-soft); cursor:pointer;
    white-space:nowrap;
  }
  .stepnav-inner button.active{ color:var(--accent); border-color:var(--accent); }

  /* ---- generic layout ---- */
  .wrap{ max-width:1140px; margin:0 auto; padding:1.5rem 1.2rem 3rem; }
  .view{ display:none; }
  .view.active{ display:block; }

  .lead{ text-align:center; max-width:640px; margin:0 auto 1.6rem; }
  .lead .hand{ font-size:1.5rem; }
  .lead p{ font-size:0.88rem; color:var(--ink-soft); }

  /* ---- section card (shared) ---- */
  .section{
    background:var(--surface); border:2px dashed var(--border); border-left:5px solid var(--accent);
    border-radius:var(--radius); padding:1.35rem 1.35rem 1.55rem; margin-bottom:1.2rem;
    box-shadow:4px 4px 0 rgba(201,138,91,0.12);
  }
  .section.sec-b{ border-left-color:var(--sec-b); box-shadow:4px 4px 0 rgba(140,134,201,0.12); }
  .section.sec-c{ border-left-color:var(--sec-c); box-shadow:4px 4px 0 rgba(226,154,154,0.14); }
  .section.sec-d{ border-left-color:var(--sec-d); box-shadow:4px 4px 0 rgba(143,185,138,0.14); }
  .section.sec-e{ border-left-color:var(--sec-e); box-shadow:4px 4px 0 rgba(127,167,201,0.14); }
  .section h2{ font-size:1.05rem; margin:0 0 0.25rem; display:flex; align-items:center; justify-content:space-between; gap:0.5rem; }
  .section .section-desc{ font-size:0.8rem; color:var(--ink-soft); margin:0 0 1.1rem; }

  fieldset{ border:none; margin:0 0 1.1rem; padding:0; }
  legend{ font-weight:700; font-size:0.85rem; margin-bottom:0.5rem; padding:0; }
  .field{ margin-bottom:1.1rem; }
  .field:last-child{ margin-bottom:0; }
  .field label{ display:block; font-weight:700; font-size:0.85rem; margin-bottom:0.35rem; }
  .hint{ font-size:0.76rem; color:var(--ink-soft); margin-top:0.3rem; }
  input[type=text], input[type=month], input[type=date], select, textarea{
    width:100%; font-family:inherit; font-size:0.92rem; color:var(--ink);
    background:#FFFDF9; border:1.5px solid var(--border); border-radius:12px;
    padding:0.6rem 0.8rem; resize:vertical;
  }
  input:focus-visible, textarea:focus-visible, select:focus-visible, button:focus-visible, .chip:focus-visible{
    outline:2px solid var(--focus); outline-offset:1px;
  }
  textarea{ min-height:4.2rem; }
  .row2{ display:grid; grid-template-columns:1fr 1fr; gap:0.8rem; }
  @media (max-width:560px){ .row2{ grid-template-columns:1fr; } }

  .chips{ display:flex; flex-wrap:wrap; gap:0.45rem; }
  .chip{
    border:1.5px solid var(--border); background:#FFFDF9; color:var(--ink);
    border-radius:999px; padding:0.4rem 0.9rem; font-size:0.82rem; cursor:pointer;
  }
  .chip[aria-pressed="true"]{ background:var(--ink); color:#fff; border-color:var(--ink); }
  .chip.small{ padding:0.28rem 0.7rem; font-size:0.76rem; }
  .chip.tagchip{ background:var(--sec-d-soft); border-color:var(--sec-d); color:#3E5A3B; }
  .chip.removable{ display:inline-flex; align-items:center; gap:0.35rem; }
  .chip.removable .x{ font-weight:700; opacity:0.6; }

  .stars{ display:flex; align-items:center; gap:0.2rem; flex-wrap:wrap; }
  .star-btn{ background:none; border:none; font-size:1.4rem; cursor:pointer; line-height:1; color:#E7DCC9; padding:0.05rem; }
  .star-btn.on{ color:var(--accent); }
  .rating-label{ font-size:0.78rem; color:var(--ink-soft); margin-left:0.4rem; }

  .btn{
    font-size:0.86rem; font-weight:700; cursor:pointer;
    border:2px solid var(--ink); border-radius:999px; padding:0.55rem 1.1rem;
    background:var(--surface); color:var(--ink); box-shadow:3px 3px 0 var(--ink);
  }
  .btn:active{ box-shadow:1px 1px 0 var(--ink); transform:translate(2px,2px); }
  .btn.primary{ background:var(--accent); color:#fff; border-color:var(--ink); }
  .btn.ghost{ box-shadow:none; border-style:dashed; }
  .btn.small{ padding:0.35rem 0.8rem; font-size:0.78rem; box-shadow:2px 2px 0 var(--ink); }
  .btn.danger{ color:#B24D4D; border-color:#B24D4D; box-shadow:3px 3px 0 #B24D4D; }
  .actions{ display:flex; gap:0.6rem; margin-top:1.2rem; flex-wrap:wrap; }

  .divider{ border:none; border-top:2px dashed var(--border); margin:1.2rem 0; }
  .callout{
    margin-top:0.4rem; background:var(--sec-c-soft); border:1.5px solid var(--border); border-radius:14px;
    padding:0.7rem 0.9rem; font-size:0.85rem;
  }
  .glossary{
    background:var(--sec-b-soft); border:1.5px solid var(--border); border-radius:14px;
    padding:0.7rem 0.9rem; font-size:0.83rem; white-space:pre-wrap;
  }
  .note-box{
    background:#FFFBF3; border:1.5px dashed var(--border); border-radius:12px; padding:0.55rem 0.8rem;
    font-size:0.82rem; color:var(--ink-soft); margin-top:0.5rem;
  }

  #toast{
    position:fixed; left:50%; bottom:1.3rem; transform:translateX(-50%) translateY(120%);
    background:var(--ink); color:#fff; padding:0.55rem 1.05rem; border-radius:999px;
    font-size:0.84rem; z-index:60; transition:transform 0.2s ease;
  }
  #toast.show{ transform:translateX(-50%) translateY(0); }

  footer{ text-align:center; font-size:0.78rem; color:var(--ink-soft); padding:0 1rem 2.2rem; }
  footer .hand{ font-size:0.95rem; }
  .visually-hidden{ position:absolute; width:1px; height:1px; overflow:hidden; clip:rect(0 0 0 0); white-space:nowrap; }

  /* ======================= HOME ======================= */
  .cycle-wrap{ display:flex; flex-direction:column; align-items:center; margin:1rem auto 2rem; }
  .cycle{ position:relative; width:300px; height:300px; max-width:80vw; max-height:80vw; }
  .cycle-ring{
    position:absolute; inset:0; border-radius:50%; border:2.5px dashed var(--accent);
    animation:spin 40s linear infinite;
  }
  @media (prefers-reduced-motion:reduce){ .cycle-ring{ animation:none; } }
  @keyframes spin{ to{ transform:rotate(360deg); } }
  .cycle-center{
    position:absolute; inset:0; display:flex; flex-direction:column; align-items:center; justify-content:center;
    text-align:center; gap:0.15rem;
  }
  .cycle-center .hand{ font-size:1.25rem; }
  .cycle-center p{ font-size:0.68rem; color:var(--ink-soft); max-width:130px; margin:0; }
  .cycle-node{
    position:absolute; width:84px; margin-left:-42px; margin-top:-24px; text-align:center;
    font-size:0.72rem; font-weight:700; color:var(--ink);
  }
  .cycle-node span{
    display:block; background:var(--surface); border:1.5px solid var(--ink); border-radius:999px;
    padding:0.35rem 0.3rem; box-shadow:2px 2px 0 var(--accent);
  }
  .stats-row{ display:flex; gap:0.8rem; flex-wrap:wrap; justify-content:center; margin-bottom:1.6rem; }
  .stat-card{
    background:var(--surface); border:1.5px solid var(--border); border-radius:14px; padding:0.7rem 1.1rem;
    text-align:center; min-width:100px;
  }
  .stat-card .num{ font-size:1.3rem; font-weight:700; color:var(--accent); font-family:"Zen Maru Gothic",sans-serif; }
  .stat-card .lbl{ font-size:0.72rem; color:var(--ink-soft); }
  .home-actions{ display:flex; flex-wrap:wrap; gap:0.7rem; justify-content:center; margin-bottom:1.6rem; }
  .home-tagline{ text-align:center; font-size:0.85rem; color:var(--ink-soft); max-width:520px; margin:0 auto 1.8rem; }

  /* ======================= COMPANY LIST / CARD ======================= */
  .grid-cards{ display:grid; grid-template-columns:repeat(auto-fill, minmax(240px,1fr)); gap:1rem; margin-bottom:1.4rem; }
  .note-card{
    position:relative; background:var(--surface); border:2.5px solid var(--ink);
    border-radius: 22px 22px 26px 26px / 28px 28px 16px 16px;
    box-shadow:4px 4px 0 var(--accent);
    padding:1.1rem 1.1rem 1.2rem; cursor:pointer; text-align:left;
  }
  .note-card:hover{ filter:brightness(1.02); }
  .note-card .nc-fox{ position:absolute; top:-6px; right:10px; width:38px; height:38px; opacity:0.9; object-fit:cover; object-position:center; border-radius:50%; }
  .note-card h3{ font-size:1.02rem; padding-right:2.6rem; }
  .note-card .nc-meta{ font-size:0.74rem; color:var(--ink-soft); margin-top:0.15rem; }
  .note-card .nc-stars{ font-size:0.95rem; color:var(--accent); margin-top:0.5rem; letter-spacing:0.05em; }
  .note-card .nc-tags{ display:flex; flex-wrap:wrap; gap:0.3rem; margin-top:0.55rem; }
  .empty-state{
    text-align:center; padding:2.2rem 1rem; color:var(--ink-soft); font-size:0.86rem;
    border:2px dashed var(--border); border-radius:var(--radius); background:var(--surface);
  }

  /* ======================= RANKING ======================= */
  .rank-item{
    display:flex; gap:1rem; align-items:flex-start; background:var(--surface); border:1.5px solid var(--border);
    border-radius:16px; padding:0.9rem 1.05rem; margin-bottom:0.8rem;
  }
  .rank-num{
    flex:none; width:36px; height:36px; border-radius:50%; background:var(--accent-soft); color:var(--accent);
    display:flex; align-items:center; justify-content:center; font-weight:700; font-family:"Zen Maru Gothic",sans-serif;
  }
  .rank-body h3{ font-size:0.98rem; }
  .rank-stars{ color:var(--accent); font-size:0.95rem; margin:0.15rem 0; }
  .rank-body .rank-top-axis{ font-size:0.76rem; color:var(--ink-soft); }
  .rank-body .rank-reason{ font-size:0.82rem; margin-top:0.3rem; }

  /* ======================= MY VALUES ======================= */
  .value-cloud{ display:flex; flex-wrap:wrap; gap:0.6rem; justify-content:center; padding:1rem 0.4rem; }
  .value-chip{
    background:var(--sec-d-soft); border:1.5px solid var(--sec-d); color:#3E5A3B; border-radius:999px;
    padding:0.4rem 0.95rem; cursor:pointer; font-weight:700; font-family:"Zen Maru Gothic",sans-serif;
  }
  .value-detail{
    background:var(--surface); border:1.5px dashed var(--border); border-radius:14px; padding:0.9rem 1rem; margin-top:1rem;
  }
  .value-detail ul{ margin:0.4rem 0 0; padding-left:1.1rem; font-size:0.85rem; }

  /* ======================= CAREER TIMELINE ======================= */
  .timeline{ position:relative; margin:1rem 0 0; padding-left:1.4rem; }
  .timeline::before{ content:""; position:absolute; left:5px; top:6px; bottom:6px; width:2px; background:var(--border); }
  .tl-item{ position:relative; margin-bottom:1.3rem; }
  .tl-item::before{
    content:""; position:absolute; left:-1.4rem; top:4px; width:12px; height:12px; border-radius:50%;
    background:var(--sec-b); border:2px solid var(--surface); box-shadow:0 0 0 2px var(--sec-b);
  }
  .tl-date{ font-size:0.74rem; color:var(--ink-soft); font-weight:700; }
  .tl-title{ font-size:0.95rem; font-weight:700; margin-top:0.1rem; }
  .tl-arrow{ color:var(--ink-soft); font-size:0.8rem; margin:0.15rem 0; }
  .tl-awareness{ font-size:0.9rem; background:var(--sec-b-soft); border-radius:10px; padding:0.5rem 0.7rem; display:inline-block; }
  .tl-tags{ margin-top:0.4rem; display:flex; gap:0.3rem; flex-wrap:wrap; }

  /* ======================= 7 DAYS ======================= */
  .cols4{ display:grid; grid-template-columns:repeat(4,1fr); gap:0.8rem; }
  @media (max-width:820px){ .cols4{ grid-template-columns:repeat(2,1fr); } }
  @media (max-width:480px){ .cols4{ grid-template-columns:1fr; } }
  .value-col{ background:var(--surface); border:1.5px solid var(--border); border-radius:14px; padding:0.8rem; }
  .value-col h4{ font-size:0.82rem; margin-bottom:0.5rem; }
  .value-col.VALUE{ border-top:5px solid var(--sec-d); }
  .value-col.STRENGTH{ border-top:5px solid var(--accent); }
  .value-col.WEAKNESS{ border-top:5px solid var(--sec-c); }
  .value-col.CHALLENGE{ border-top:5px solid var(--sec-e); }
  .value-col .chips{ margin-bottom:0.5rem; min-height:1.6rem; }
  .value-col .mini-add{ display:flex; gap:0.35rem; }
  .value-col .mini-add input{ font-size:0.78rem; padding:0.35rem 0.5rem; }

  .recap-page{ background:var(--surface); border:2.5px dashed var(--border); border-radius:var(--radius); padding:1.5rem; }
  .recap-page h3{ text-align:center; font-size:1.2rem; margin-bottom:0.2rem; }
  .recap-page .recap-sub{ text-align:center; color:var(--ink-soft); font-size:0.8rem; margin-bottom:1.2rem; }
  .recap-block{ margin-bottom:0.9rem; }
  .recap-block .rb-label{ font-size:0.74rem; font-weight:700; color:var(--ink-soft); }
  .recap-block .rb-body{ font-size:0.88rem; }
  .recap-final{ text-align:center; margin-top:1.6rem; }
  .recap-final .step{
    opacity:0; transform:translateY(6px); transition:opacity .5s ease, transform .5s ease;
    font-size:0.95rem; margin:0.3rem 0;
  }
  .recap-final .step.show{ opacity:1; transform:none; }
  .recap-final .step.final{ font-family:"Zen Maru Gothic",sans-serif; font-weight:700; font-size:1.1rem; color:var(--accent); }

  /* ======================= WORD CLOUD ======================= */
  .wc-input-row{ display:flex; gap:0.6rem; flex-wrap:wrap; }
  .wc-input-row input{ flex:1; min-width:200px; }
  .wc-stage{
    position:relative; margin-top:1.2rem; height:380px; border:2px dashed var(--border); border-radius:var(--radius);
    background:var(--sec-e-soft); overflow:hidden;
  }
  .wc-stage.dim::after{
    content:""; position:absolute; inset:0; background:rgba(74,63,53,0.55); z-index:5;
  }
  .wc-word{
    position:absolute; font-family:"Zen Maru Gothic",sans-serif; font-weight:700; color:var(--ink);
    white-space:nowrap; animation: popfloat 6s ease-in-out infinite; opacity:0.92;
  }
  @keyframes popIn{ 0%{ transform:scale(0.3); opacity:0; } 70%{ transform:scale(1.08); opacity:1; } 100%{ transform:scale(1); opacity:0.92; } }
  @keyframes popfloat{ 0%,100%{ transform:translateY(0); } 50%{ transform:translateY(-8px); } }
  .wc-reveal{
    position:absolute; inset:0; z-index:10; display:flex; align-items:center; justify-content:center; text-align:center;
    flex-direction:column; gap:0.5rem; padding:1rem;
  }
  .wc-reveal .step{
    opacity:0; transform:translateY(6px); transition:opacity .5s ease, transform .5s ease;
    color:#fff; font-size:1.05rem; font-weight:700; font-family:"Zen Maru Gothic",sans-serif;
  }
  .wc-reveal .step.show{ opacity:1; transform:none; }
  .wc-reveal .step.big{ font-size:1.5rem; }
  .wc-actions{ margin-top:1rem; text-align:center; }
  .wc-note{ font-size:0.74rem; color:var(--ink-soft); text-align:center; margin-top:0.6rem; }

  /* ======================= DEEP DIVE ======================= */
  .deepdive-box{
    background:var(--sec-e-soft); border:1.5px dashed var(--sec-e); border-radius:12px; padding:0.7rem 0.85rem;
    font-size:0.8rem; margin-top:0.5rem;
  }
  .deepdive-box ul{ margin:0.4rem 0 0; padding-left:1.1rem; }
  .axis-block{ border:1.5px solid var(--border); border-radius:14px; padding:0.8rem 0.9rem; margin-bottom:0.8rem; background:#FFFDF9; }
  .axis-block .axis-head{ display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:0.4rem; }
  .axis-block .axis-head strong{ font-size:0.88rem; }

  @media print{
    header, nav.stepnav, .home-actions, .actions, footer, .stepnav{ display:none !important; }
    .wrap{ padding:0; }
    body{ background:#fff; }
  }
</style>
</head>
<body>

<header>
  <div class="header-inner">
